<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tfjt.pay.external.unionpay.dao.LoanUserDao">
    <select id="applicationStatusNotSucceededData" resultType="com.tfjt.pay.external.unionpay.entity.LoanUserEntity">
        SELECT
            *
        FROM
            tf_loan_user
        WHERE
            (application_status != 'succeeded' or application_status is null)
          AND out_request_no IS NOT NULL
        ORDER BY
            create_date DESC
    </select>
    <select id="listLoanUserByBusId"
            resultType="com.tfjt.pay.external.unionpay.dto.resp.UnionPayLoanUserRespDTO">
        select  a.balance_acct_id, u.bus_id,u.name as balanceAcctName   from tf_loan_user u JOIN tf_loan_balance_acct a on u.id = a.loan_user_id
        where a.id in
        <foreach collection="list" separator="," open="(" close=")" item="item">
            #{item}
        </foreach> and a.type = #{type}

    </select>
    <select id="getByBalanceAcctId" resultType="com.tfjt.pay.external.unionpay.entity.LoanUserEntity">
        SELECT
            id,
            type,
            bus_id,
            creator
        FROM
            tf_loan_user
        WHERE id = (select loan_user_id from tf_loan_balance_acct where balance_acct_id=#{parameter})
    </select>

</mapper>
